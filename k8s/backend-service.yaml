apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: bikeflow
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: bikeflow
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 8000
      targetPort: http

